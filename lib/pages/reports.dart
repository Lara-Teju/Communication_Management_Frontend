import 'package:flutter/material.dart';

class ReportsPlaceholderPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Generate reports'),
      ),
      body: Center(
        child: Text('reports feature is under development.'),
      ),
    );
  }
}
