import 'package:flutter/material.dart';

class FeedbackPlaceholderPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Generate feedback'),
      ),
      body: Center(
        child: Text('feedback feature is under development.'),
      ),
    );
  }
}
